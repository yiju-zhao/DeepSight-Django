import type { Config } from 'postcss-load-config';

// ============================================================================
// POSTCSS CONFIGURATION
// ============================================================================

const config: Config = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};

export default config;
